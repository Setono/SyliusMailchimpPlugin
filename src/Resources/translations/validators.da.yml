setono_sylius_mailchimp:
    list:
        config:
            not_blank: Konfigurationen skal udfyldes.
        name:
            not_blank: Navn skal udfyldes.
            min_length: Navn skal minimum være {{ limit }} karakterer.
            max_length: Navn må ikke være længere end {{ limit }} karakterer.
        list_id:
            unique: Der er allerede en liste med dette ID.
            not_blank: Liste ID'et skal udfyldes.
            min_length: Liste ID'et skal være minimum {{ limit }} karakterer.
            max_length: Liste ID'et må ikke være længere end {{ limit }} karakterer.
        store_id:
            min_length: Navn skal minimum være {{ limit }} karakterer.
            max_length: Navn må ikke være længere end {{ limit }} karakterer.
            regex: Butiks-id kan kun bestå af bogstaver, tal, bindestreger og understreger.
