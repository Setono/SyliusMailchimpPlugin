<?xml version="1.0" encoding="UTF-8"?>

<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      http://doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="Setono\SyliusMailChimpPlugin\Entity\MailChimpConfig" table="setono_mailchimp_export_config">
        <id name="id" type="integer">
            <generator strategy="AUTO"/>
        </id>

        <field name="code" type="string" nullable="true" unique="true"/>
        <field name="apiKey" type="string" nullable="true"/>
        <field name="exportAll" type="boolean" nullable="false"/>

        <one-to-many field="lists"
                     target-entity="Setono\SyliusMailChimpPlugin\Entity\MailChimpListInterface"
                     mapped-by="config" orphan-removal="true">
            <cascade>
                <cascade-all/>
            </cascade>
        </one-to-many>
    </entity>

</doctrine-mapping>
