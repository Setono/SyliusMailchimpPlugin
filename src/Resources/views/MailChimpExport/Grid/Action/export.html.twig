{% import '@SyliusUi/Macro/buttons.html.twig' as buttons %}

{% set path = path('setono_sylius_mailchimp_export_plugin_admin_export_to_mailchimp') %}

{{ buttons.default(path, action.label, 'setono-mailchimp-export', 'arrow up', 'blue') }}
